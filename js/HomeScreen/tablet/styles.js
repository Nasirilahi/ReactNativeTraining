import {
    StyleSheet,
    Platform
} from 'react-native';
import commonstyles from '../HomeScreenStyles'
export default  StyleSheet.create({
    ...commonstyles,
    container: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    }
});
